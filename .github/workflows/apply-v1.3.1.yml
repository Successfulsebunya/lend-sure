name: Apply v1.3.1 source

on:
  push:
    branches:
      - release/v1.3.1

jobs:
  apply:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by apply-v1.3.1-fixed.yml"
